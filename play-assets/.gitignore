# Screenshots captured off a live device show real pool names, usernames and
# predictions. Play listings are public, so these stay out of the repo — retake
# them from a pool created with neutral names before uploading.
shot-*.png
