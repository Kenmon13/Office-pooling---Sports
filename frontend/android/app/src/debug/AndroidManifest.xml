<?xml version="1.0" encoding="utf-8" ?>
<!-- Debug-only overlay. Lets the debug build talk to a backend running on the
     developer's machine over plain HTTP (http://10.0.2.2:3001 from the emulator).
     Release builds never merge this file, so the shipped AAB keeps Android's
     default of refusing cleartext traffic. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic" />
</manifest>
